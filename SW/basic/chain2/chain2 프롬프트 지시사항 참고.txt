chain2 프롬프트 지시사항 참고

사용자 목적에 맞는 최적 배치를 구성해야 한다.

시트의 **위치(x, y), 형태(type), 방향(direction)**을 종합적으로 판단해야 한다.

공간 효율성과 사용 편의성을 고려하여 어떤 시트를 움직일지 GPT가 자율적으로 판단한다.

생성된 배치는 seat_1(type, x, y, direction) 형식으로 표현된다.

방향(direction)은 자유롭게 생성한다.

최종 출력은 Chain-3가 받아 물리적으로 구현 가능한 상태여야 한다.