[user]
시트 제어에 필요한 동작들은 다음과 같이 정의됩니다:
"""
"시트 제어 동작 리스트"

# 이동 동작
- disk_rotate(degree):
  바닥 회전판을 회전시킵니다. 레일은 회전판 중심선을 따라 내장되어 있습니다.  
  degree ∈ {90}.
  기능: rail_axis를 "x"에서 "y"로 전환하고, 시트 방향을 시계방향으로 90도 회전시킵니다.
          F→R, R→B, B→L, L→F. position(A/M/C)은 변하지 않습니다.

- move_on_rail(target):
  내장된 레일을 따라 시트를 절대 위치로 이동시킵니다. 
  target ∈ {"A","M","C"}  (A=음의 끝점, M=중앙, C=양의 끝점).  
  기능: position(A/M/C)만 변경됩니다. rail_axis와 facing은 변하지 않습니다.

- seat_rotate(degree):
  시트 본체를 차량 기준으로 회전시킵니다.  
  degree ∈ {90, 180, 270}.
  기능: facing(F/R/B/L)만 변경됩니다. rail_axis와 position(A/M/C)은 변하지 않습니다.
 
#상태 동작
- unfold(id):
  시트 모드를 "chair"로 변경합니다. 모든 이동 동작 후 시트를 펼칠 때 사용합니다.

- fold(id):
  시트 모드를 "storage"로 변경합니다. (공간 확보를 위해 시트를 수납하는 경우 사용).

#기타 동작
- unchanged
  이동 및 상태 동작이 필요하지 않은 경우 사용합니다. 
  **초기 상태 == instruction인 경우 사용합니다.**
"""
-------------------------------------------------------
위의 텍스트는 전체 지침의 일부입니다. 아직 작업을 시작하지 마세요.
[assistant]
이해했습니다. 다음 입력을 기다리는 중입니다.
