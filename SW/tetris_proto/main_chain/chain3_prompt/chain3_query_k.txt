작업을 시작하세요. 지침은 다음과 같습니다:
"""
"instruction": {[INSTRUCTION]}
"""
다음 규칙을 따르세요:
1. 정확히 하나의 구조화된 형식만 반환하세요(자유 텍스트 금지 / 코드 펜스 금지).
2. **예시에 표시된 출력 형식을 정확히 따르며, 불필요한 공백이나 추가 줄바꿈을 넣지 마세요.**
3. Seats는 반드시 중괄호로 감싼 JSON 객체여야 하며, 문자열로 작성하면 안 됩니다. 모든 키와 문자열은 큰따옴표(" ")를 사용하고, 내부 객체도 문자열로 감싸지 마세요.
4. 출력은 Python json.loads로 그대로 파싱 가능한 단일 JSON 객체여야 합니다. trailing commas, comments, NaN/Infinity는 허용되지 않으며, 모든 중괄호와 대괄호의 짝이 정확히 맞아야 합니다.
5. 좌석 제어 시퀀스는 다음 제약을 따라야 합니다:
- "facing"이 F→R 또는 R→B 또는 B→L 또는 L→F로 바뀌면 "degree"는 90
- "facing"이 F→L 또는 L→B 또는 B→R 또는 R→F로 바뀌면 "degree"는 270
- "facing"이 F→B 또는 B→F 또는 R→L 또는 L→R로 바뀌면 "degree"는 180
- **각 좌석의 동작 시퀀스 내부에서는 이 순서를 따르세요: disk_rotate → fold → seat_rotate → move_on_rail → unfold.**
  (이것은 동작 순서에 대한 규칙이지, 모든 함수를 반드시 사용하라는 요구가 아닙니다.)
- task_sequence는 최소 유효 시퀀스여야 합니다(불필요한 동작 금지).
6. 최종 응답을 생성하기 전, 다음을 검증하세요:
- **keys(instruction.seats) == keys(task_sequence)**
- **모든 중괄호 {} 개수가 정확히 맞는지 검증하세요.**

**작업은 단계적으로 진행해야 합니다. 각 동작 후 상태를 갱신하고, 갱신된 상태를 기준으로 다음 동작을 결정하세요.**
위 규칙을 따르세요. Think step by step.

